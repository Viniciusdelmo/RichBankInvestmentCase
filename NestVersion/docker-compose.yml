services:
  postgres:
    image: postgres:16-alpine
    container_name: richbank-investment-nest-postgres
    environment:
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: richbank-nest
    ports:
      - 5432:5432
    volumes:
      - richbank-nest-postgres-data:/var/lib/postgresql/data

volumes:
  richbank-nest-postgres-data:
    driver: local